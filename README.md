# StegiT

This project implements a Generative Adversarial Network (GAN) from scratch using TensorFlow/Keras to synthesize 28×28 grayscale images from latent noise vectors. The architecture consists of a fully connected generator network that maps 100-dimensional Gaussian noise to the image domain, and a discriminator network to perform binary classification
